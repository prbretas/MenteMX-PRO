{
  "name": "@mentemx/core",
  "version": "0.1.0",
  "private": true,
  "description": "MenteMX Pro - Core business logic (analytics, streak, formatting)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.3.2",
    "vitest": "^1.1.0",
    "fast-check": "^3.15.0",
    "rimraf": "^5.0.5",
    "eslint": "^8.55.0"
  }
}
