{
  "name": "@mentemx/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "MenteMX Pro - Desktop App (Electron + React)",
  "main": "dist/main.js",
  "scripts": {
    "dev": "echo 'Desktop app - setup pending (issue #42)'",
    "build": "echo 'Desktop app - setup pending (issue #42)'",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.2",
    "vitest": "^1.1.0"
  }
}
